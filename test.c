#include <stdio.h>
int main(){
    // int a = 0;
    // a = a++ + a++ - a++ + ++a;
    // printf("%d", a);

    int i;
    for(i = 0; i < 5; i++)
    {
        printf(" (%d) ", i);
        int i = 10;

        printf("%d ", i);

        i++;
        printf("{%d }", i);
    }
    
    
return 0;
}